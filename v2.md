# todos

## editing
- check all the entry actions from v1 based on the status of the entry
- display a banner if an entry is untranslated and let the user select another
  language to start translating from. add the translate button to the top header
  which creates the tranlated entry for that language. should be functionally the
  same as v1

## users
- bring back the login functionality from CloudAuthView if we're not in dev mode (but can be renamed to a more generic name because it also used for self hosted)
- bring back the view that shows we are deployed but without valid handler from v1
- check the policy atom where necessary

## history
- implement the history panel on the right sidebar, put the history state in 
  a DashboardEntry atom that retrieves it via connection, see v1 for what to display
- make sure selecting a history entry shows the correct data in readonly mode and updates
  the preview panel with that data too

## responsive
- make both sidebars collapsible and collapse them by default on mobile
- do necessary css adjustments to make the UI work on mobile, see v1 for reference

## rich text fields
- port over the styling for all the items within the rich text field
- make sure the toolbar works correctly, including picking entries for internal links

## other
- display icons in tabs view
- fix the right sidebar header to center items vertically
