export enum HandleAction {
  Auth = 'auth',
  User = 'user',
  Resolve = 'resolve',
  Pending = 'pending',
  Sync = 'sync',
  Draft = 'draft',
  History = 'history',
  PreviewToken = 'previewToken',
  Mutate = 'mutate',
  Upload = 'upload',
  Tree = 'tree',
  Commit = 'commit',
  Blob = 'blob'
}
