{
  "private": true,
  "name": "@alinea/demo",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "yarn build:alinea && alinea serve -- next dev",
    "generate": "yarn build:alinea && alinea generate",
    "build": "yarn build:alinea && alinea generate -- next build",
    "start": "next start"
  },
  "dependencies": {
    "@alinea/content": "link:.alinea",
    "alinea": "0.0.0",
    "include-media": "^1.4.10",
    "next": "^12.2.5",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-frame-component": "^5.2.3"
  },
  "devDependencies": {
    "@svgr/webpack": "^6.3.1",
    "@types/react": "^18.0.5",
    "@types/react-dom": "^18.0.0"
  }
}
