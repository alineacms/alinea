{
  "name": "@alineacms/website",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "yarn workspace @alineacms/root build && alinea generate && next dev",
    "build": "alinea generate && next build",
    "generate": "alinea generate",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@alineacms/auth.passwordless": "0.0.0",
    "@alineacms/backend": "0.0.0",
    "@alineacms/cli": "0.0.0",
    "@alineacms/core": "0.0.0",
    "@alineacms/dashboard": "0.0.0",
    "@alineacms/ui": "0.0.0",
    "next": "12.1.0"
  },
  "devDependencies": {
    "@types/find-config": "^1.0.1",
    "cross-env": "^7.0.3",
    "find-config": "^1.0.0",
    "postcss-pxtorem": "^6.0.0"
  }
}
